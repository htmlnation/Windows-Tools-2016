 <html>
<head>
<title>color</title>
<HTA:APPLICATION
 ID="objTest"
     APPLICATIONNAME="sinstall"
     SCROLL="NO"
     INNERBORDER="no"
     borderStyle"sunken"
     SINGLEINSTANCE="yes"
     SHOWINTASKBAR="no"
     CAPTION="no"
     icon = "C:\Temp\g.ico"

>
</head>

<SCRIPT LANGUAGE="VBScript">

     Dim objShell :Set objShell = CreateObject("WScript.Shell")
        Dim dtmStartTime
 window.resizeTo 50,50
        window.moveTo intmid
 dtmStartTime = Now
        idTimer = window.setTimeout("Start", 100, "VBScript")

    Sub Start
       window.document.body.background = "a.jpg"
       dtmStartTime = Now
       idTimer = window.setTimeout("color1", 5000, "VBScript")
    End Sub
 
    Sub color1
        window.document.body.background = "b.jpg"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color2", 5000, "VBScript")
    End Sub

    Sub color2
        window.document.body.background = "c.jpg"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color3", 5000, "VBScript")
    End Sub

    Sub color3
        window.document.body.background = "d.jpg"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color4", 5000, "VBScript")
    End Sub

   Sub color4
        window.document.body.background = "e.jpg"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color5", 5000, "VBScript")
    End Sub

    Sub color5
       window.document.body.background = "f.jpg"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color6", 5000, "VBScript")
    End Sub

    Sub color6
        window.document.body.background = "g.jpg"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color7", 5000, "VBScript")
    End Sub

    Sub color7
        window.document.body.background = "h.jpg"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color8", 5000, "VBScript")
    End Sub

    Sub color8
        document.body.bgColor="#800000"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color9", 5000, "VBScript")
    End Sub

    Sub color9
        document.body.bgColor="#008000"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color10", 5000, "VBScript")
    End Sub

    Sub color10
        document.body.bgColor="#000080"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color11", 5000, "VBScript")
    End Sub

    Sub color11
        document.body.bgColor="#808000"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color12", 5000, "VBScript")
    End Sub

    Sub color12
        document.body.bgColor="#800080"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color13", 5000, "VBScript")
    End Sub

    Sub color13
        document.body.bgColor="#008080"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color14", 5000, "VBScript")
    End Sub

    Sub color14
        document.body.bgColor="#C0C0C0"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color15", 5000, "VBScript")
    End Sub

    Sub color15
        document.body.bgColor="#808080"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color16", 5000, "VBScript")
    End Sub

    Sub color16
        document.body.bgColor="#9999FF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color17", 5000, "VBScript")
    End Sub

    Sub color17
        document.body.bgColor="#993366"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color18", 5000, "VBScript")
    End Sub

    Sub color18
        document.body.bgColor="#FFFFCC"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color19", 5000, "VBScript")
    End Sub

    Sub color19
        document.body.bgColor="#CCFFFF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color20", 5000, "VBScript")
    End Sub

    Sub color20
        document.body.bgColor="#660066"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color21", 5000, "VBScript")
    End Sub

    Sub color21
        document.body.bgColor="#FF8080"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color22", 5000, "VBScript")
    End Sub

    Sub color22
        document.body.bgColor="#0066CC"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color23", 5000, "VBScript")
    End Sub

    Sub color23
        document.body.bgColor="#CCCCFF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color24", 5000, "VBScript")
    End Sub

    Sub color24
        document.body.bgColor="#FF00FF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color25", 5000, "VBScript")
    End Sub

    Sub color25
        document.body.bgColor="#FFFF00"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color26", 5000, "VBScript")
    End Sub

    Sub color26
        document.body.bgColor="#00FFFF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color27", 5000, "VBScript")
    End Sub

    Sub color27
        document.body.bgColor="#800080"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color28", 5000, "VBScript")
    End Sub

    Sub color28
        document.body.bgColor="#800000"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color29", 5000, "VBScript")
    End Sub

    Sub color29
        document.body.bgColor="#008080"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color30", 5000, "VBScript")
    End Sub

    Sub color30
        document.body.bgColor="#0000FF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color31", 5000, "VBScript")
    End Sub

    Sub color31
        document.body.bgColor="#00CCFF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color32", 5000, "VBScript")
    End Sub

    Sub color32
        document.body.bgColor="#CCFFFF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color33", 5000, "VBScript")
    End Sub

    Sub color33
        document.body.bgColor="#CCFFCC"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color34", 5000, "VBScript")
    End Sub

    Sub color34
        document.body.bgColor="#FFFF99"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color35", 5000, "VBScript")
    End Sub
 
    Sub color35
        document.body.bgColor="#99CCFF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color36", 5000, "VBScript")
    End Sub

    Sub color36
        document.body.bgColor="#FF99CC"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color37", 5000, "VBScript")
    End Sub

    Sub color37
        document.body.bgColor="#CC99FF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color38", 5000, "VBScript")
    End Sub

    Sub color38
        document.body.bgColor="#FFCC99"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color39", 5000, "VBScript")
    End Sub

    Sub color39
        document.body.bgColor="#3366FF"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color40", 5000, "VBScript")
    End Sub

    Sub color40
        document.body.bgColor="#33CCCC"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color41", 5000, "VBScript")
    End Sub

    Sub color41
        document.body.bgColor="#99CC00"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color42", 5000, "VBScript")
    End Sub

    Sub color42
        document.body.bgColor="#FFCC00"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color43", 5000, "VBScript")
    End Sub

    Sub color43
        document.body.bgColor="#FF9900"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color44", 5000, "VBScript")
    End Sub

    Sub color44
        document.body.bgColor="#FF6600"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color45", 5000, "VBScript")
    End Sub

    Sub color45
        document.body.bgColor="#666699"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color46", 5000, "VBScript")
    End Sub

    Sub color46
        document.body.bgColor="#969696"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color47", 5000, "VBScript")
    End Sub

    Sub color47
        document.body.bgColor="#003366"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color48", 5000, "VBScript")
    End Sub

    Sub color48
        document.body.bgColor="#339966"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color49", 5000, "VBScript")
    End Sub

    Sub color49
        document.body.bgColor="#003300"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color50", 5000, "VBScript")
    End Sub

    Sub color50
        document.body.bgColor="#333300"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color51", 5000, "VBScript")
    End Sub

    Sub color51
        document.body.bgColor="#993300"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color52", 5000, "VBScript")
    End Sub

    Sub color52
        document.body.bgColor="#993366"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color53", 5000, "VBScript")
    End Sub

    Sub color53
        document.body.bgColor="#333399"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("color54", 5000, "VBScript")
    End Sub

    Sub color54
        document.body.bgColor="#333333"
        window.clearTimeout(idTimer)
 dtmStartTime = Now
        idTimer = window.setTimeout("Start", 5000, "VBScript")
    End Sub

   
    Sub CloseWindow
       If Window.Event.KeyCode= 27 Then 
       self.close
       End if 
    End Sub

 
 

</SCRIPT>

<body onkeypress='CloseWindow'>


</body>
</html>